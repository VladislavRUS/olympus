import styled from 'styled-components';

export const Wrapper = styled.div`
  padding: 10px 20px;
  cursor: pointer;
  text-align: left;

  &:hover {
    background-color: rgba(0, 0, 0, 0.05);
  }
`;
